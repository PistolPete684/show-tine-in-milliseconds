// function past(h, m, s){
//   // Multiple the variable by milliseconds and add them together
//   return (h * 3600000) + (m * 60000) + (s * 1000);
  
// }

// Multiple the variable by milliseconds and add them together
past = (h, m, s) => (h * 3600000) + (m * 60000) + (s * 1000);